//the shell sudo-code 
<dirent.h>
<unistd.h>
<system.h>
var user_input;
var is_running = 1;


function token(string){
    var cut; 
    var new_words = token(cut,string);
    return string;
}

function categorizing(word_already_token){
        switch(word_already_token){
            case :arguments_word {

            }

            case: command{
                
            }
            case: type_of_sign{
                switch(var x ){
                    case > 
                    case <
                    case >>
                    case << 
                    case |
                    case - 
                }
            }
        }
        
}

function order_runner(){
    if(argument_before_or_after_the_word);
}

function cd(argument){
    chdir();
    if(success){

    }else{

    }
}
function clr(){
 // printf magic code 
}
function dir(){
opendir()
readdir()
    if(1){
        print_name;
    } else{

    }
}
function environ(){
    printf(enviroment)// listing 
}
function repeat_running(){
    while(is_running){
        dosomething();
    }
         quit();
}
 
function help(){
printf(all_help_information)
}
function pause(){
is_running = 0;
}
function quit(){
    is_running = 0;
exit()
}
function dosomething(){
    getline();
}

function clear(){
    printf("\033[H\033[2J");
}
function echo(string){
    printf(string);
}

